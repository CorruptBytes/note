# 脚手架工具

React官方提供了`create-react-app`的脚手架，可以快速初始化React项目模板。


```
npx create-react-app my-app
```


# JSX/TSX

`JSX`和`TSX`是React提供的在JS/TS代码中编写类似HTML的语法，，用于快速书写组件HTML骨架，并支持嵌入模板语法。
- JSX和TSX本质是一种快速定义HTML骨架的语法糖，最终仍然会被编译为纯JS/TS代码执行。
<h3>使用形式</h3>
```
const element = <h1>Hello World</h1>;
```
- JSX/TSX要求所有标签(无论是单标签还是双标签)都要正确闭合
- 对于多行HTML，必须用`()`包裹，对于单行HTML,可以直接书写，但建议均使用`()`包裹以标识。
- `Fragment`
<h3>原理</h3>
最终HTML模板会被编译为
```
const element = React.createElement(
    "h1",
    null,
    "Hello World"
);
```

## 模板语法

模板语法允许在页面模板（HTML骨架）中嵌入数据和逻辑,从而让页面能够根据数据动态生成。
React没有提供独立的模板语法体系，而是在JS/TS嵌入HTML的基础上，尽量使用原生JS/TS语法实现模板语法的效果。
<h3>插值表达式</h3>
插值表达式可以把变量、表达式的值插入到页面模板中进行显示。
```
const title = '标题'

<h1>{title}</h1>
```
- 插值表达式既可以用在标签内容中，也可以应用于标签属性上。

<h3>条件渲染</h3>
<h3>列表渲染</h3>
<h3>事件绑定</h3>
<h3>属性绑定</h3>

## 状态驱动渲染
对于普通的变量，变量值变化并不会导致页面重新渲染。想要实现页面渲染随着变量更新变化，需要声明状态:
```
const[count,setCount] = useState(0)
```

# 组件定义

React提供了两种组件定义方式：
- 函数式组件 ：目前主流使用，语法简单便捷。
- 类组件 ： 语法复杂且冗余。
## 函数式组件

通过函数返回值定义组件
```

```
- 组件只能包含一个根元素，否则编译出错
- React提供了`<>`空标签处理需要存在多个根元素的组件，在组件最外层添加`<>`包裹组件，`<>`在渲染时不会生成任何额外标签