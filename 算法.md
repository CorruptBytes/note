# 二分查找(Binary Search)

> 又称二分法，是从一个有序数组中找到一个`target`值的搜索算法

- 核心思想

  > 由于数组是有序的，可以设定一个区间，每次比对区间中间的值并根据值与target的相对大小缩小区间，每次可以缩小一半的区间，所以称为二分法。

## 实现

> 根据标识区间范围的两个数为左闭右开和左闭右闭，可以将二分法分为两种写法

- 左闭右闭

  ```java
  public static int BinarySearch(int[] array,int target) {
          int indexMin = 0;
          int indexMax = array.length - 1;
          while (indexMin <= indexMax) {
              int indexTemp = (indexMax + indexMin) / 2;
              if (array[indexTemp] > target) {
                  indexMax = indexTemp - 1;
              } else if (array[indexTemp] == target) {
                  return indexTemp;
              } else if (array[indexTemp] < target) {
                  indexMin = indexTemp + 1;
              }
          }
          return -1;
      }
  ```

- 左闭右开

  ```
  public static int BinarySearch(int[] array,int target) {
          int indexMin = 0;
          int indexMax = array.length;
          while (indexMin < indexMax) {
              int indexTemp = (indexMax + indexMin) / 2;
              if (array[indexTemp] > target) {
                  indexMax = indexTemp;
              } else if (array[indexTemp] == target) {
                  return indexTemp;
              } else if (array[indexTemp] < target) {
                  indexMin = indexTemp + 1;
              }
          }
          return -1;
      }
  ```

对于循环判断条件和边界值的更新，要保持与初始区间一致的设定以及合法性，如果初始设定区间为左闭右闭则后续更新区间时也必须是左闭右闭

## 二分答案

**二分答案**是一种对答案空间进行二分查找的算法思想。

<h4>应用场景</h4>

二分答案常用于优化具有单调性的问题，也就是说问题的可行性在一个区间内呈现单调变化，此时相比于遍历区间寻找目标答案，通过二分查找的思想可以快速缩小区间，具有更优异的性能。

可以使用二分答案求解的问题具有以下特点：

- 答案在一个区间内
- 问题的可行性在区间内具有单调性，即区间内存在一个临界值，使得在该临界值两侧可行性发生一次且仅一次翻转
- 可以较容易地判断一个答案是否可行

<h4>模板</h4>

```java
while (l <= r) {
    int mid = (l + r) / 2;

    if (check(mid)) {
        // mid可行
        更新区间
    } else {
        // mid不可行
        更新区间
    }
}
```

`check()`是一个可行性函数，用以判断当前答案`mid`是否可行，从而更新区间。

# 最小子数组

> 给定一个含有 `n` 个正整数的数组和一个正整数 `target` **。**
>
> 找出该数组中满足其总和大于等于 `target` 的长度最小的 **子数组** `[numsl, numsl+1, ..., numsr-1, numsr]` ，并返回其长度**。**如果不存在符合条件的子数组，返回 `0` 。

- 核心思想

  > 可以使用一个滑动窗口扫描数组，并根据滑动窗口所框住子数组的和来动态的移动和改变滑动窗口的大小和位置，本质仍然是双指针。

```java
public int minSubArrayLen(int target, int[] nums) {
        int lowPtr = 0;
        int highPtr = 0;
        int sum = 0;
        int result = 0;
       for (;highPtr < nums.length; highPtr++) {
           sum += nums[highPtr];
           while (sum >= target) {
               int subLength = highPtr - lowPtr + 1;
               result = result != 0 ? Math.min(subLength,result) : subLength;
               sum -= nums[lowPtr];
               lowPtr++;

           }
       }
       return result;
    }
```

# 螺旋矩阵

> 一个正整数 `n` ，生成一个包含 `1` 到 `n2` 所有元素，且元素按顺时针顺序螺旋排列的 `n x n` 正方形矩阵 `matrix` 。

- 核心思想

  > 将二维数组看成一个矩阵，以每螺旋一圈为循环，在这个循环中又以边为单位进行处理。通过定义矩阵坐标，以螺旋的形式更新矩阵坐标并赋值。

```java
 public int[][] generateMatrix(int n) {
        int startX = 0;
        int startY = 0;
        int loopCount = 1;
        int count = 1;
        int[][] nums = new int[n][n];

        while (loopCount <= n / 2) {
            int j = startY;
            int i = startX;
            //上边
            for (; j < n -loopCount;j++) {
                nums[i][j] = count;
                count++;
            }
            //右边
            for (;i < n - loopCount;i++) {
                nums[i][j] = count;
                count++;
            }
            //下边
            for (;j > startY;j--) {
                nums[i][j] = count;
                count++;
            }
            //左边
            for (;i > startX;i--) {
                nums[i][j] = count;
                count++;
            }
            loopCount++;
            startX++;
            startY++;
        }
        if (n % 2 == 1) {
            nums[startX][startY] = n * n;
        }
        return nums;
    }
```

> 注意:
>
> - 要以相同的方式处理每一边，即以相同的方式看待相邻边共同节点的归属权。
> - 如果螺线矩阵的边长为基数，此时最中心有个元素无法构成一圈，因此单独判断进行赋值

# KMP算法

> KMP算法是用来判断一个字符串中是否含有另一个字符串的算法。名字来源于KMP算法三名发明者的名字首字母。

- 前缀与后缀

  > 对于字符串而言，前缀是包含首字母不包含尾字符的所有字串，后缀是包含尾字符不包含首字符的所有字串。

- 前缀表

  > 前缀表是由一个字符串及其字串的最长相当前后缀的长度列出的表

# 逆波兰表达式

> 逆波兰表达式是一种后缀表达式，

- 中缀表达式

  > 符合人类阅读的数学表达式，如(1+2) * （3+ 4）。实际可将表达式写成一个二叉树，中缀表达式是此二叉树中序遍历后的结果。中缀表达式想要计算出正确结果，必须加上括号来表现优先级。

- 后缀表达式

  > 将中缀表达式写成二叉树，进行后序遍历的结果。后缀表达式直接由前向后顺序计算即可得到正确结果

  ![image-20250801095156144](D:\笔记\图片\image-20250801095156144.png)

# 滑动窗口最大值

# 回溯算法

> 回溯隐藏在递归之下，回溯是依赖于递归的。回溯函数就是一个特殊的递归函数

回溯是一个递归的过程，可以将回溯的过程抽象为一个树形结构

这个树的横向传播通过迭代(for循环),纵向传播通过递归

- 回溯模板

  > 回溯函数通常无返回值，且具有较多的参数

```
void backtracking(参数) {
	if (终止条件) {
		收集结果
		return;
	}
	//for循环遍历集合元素
	for() {
		处理每一个节点
		递归函数
		回溯操作，撤销对节点的处理
	}
}
```

## N皇后

> 按照国际象棋的规则，皇后可以攻击与之处在同一行或同一列或同一斜线上的棋子。
>
> **n 皇后问题** 研究的是如何将 `n` 个皇后放置在 `n×n` 的棋盘上，并且使皇后彼此之间不能相互攻击。

- 回溯

  > 通过回溯，按行递归，按列迭代，暴力求解

  ```java
  public List<List<String>> solveNQueens(int n) {
          List<List<String>> results = new ArrayList<>();
          List<String> result = new ArrayList<>();
          StringBuilder row = new StringBuilder();
          row.append(".".repeat(Math.max(0, n)));
          for (int i = 0; i < n; i++) {
              result.add(row.toString());
          }
          backtracking12(results,result,n,0);
          return results;
      }
      public void backtracking12(List<List<String>> results,List<String> result,int n,int row) {
          if (row == n) {
              results.add(new ArrayList<>(result));
              return;
          }
          for (int i = 0; i < n; i++) {
              if (!isValid(result,row,i,n)) {
                  continue;
              }
              String s = result.get(row);
              StringBuilder sb = new StringBuilder(s);
              sb.setCharAt(i,'Q');
              result.set(row,sb.toString());
              backtracking12(results,result,n,row + 1);
              sb.setCharAt(i,'.');
              result.set(row,sb.toString());
          }
      }
  ```


## 解数独

数独的解法需 **遵循如下规则**：

1. 数字 `1-9` 在每一行只能出现一次。
2. 数字 `1-9` 在每一列只能出现一次。
3. 数字 `1-9` 在每一个以粗实线分隔的 `3x3` 宫内只能出现一次。

```java
    public void solveSudoku(char[][] board) {
        backtracking(board);
    }
//检查遇到的第一个空格，并向里面填数
    private boolean backtracking(char[][] board) {
        for (int row = 0; row < 9; row++) {
            for (int col = 0; col < 9; col++) {
                if (board[row][col] != '.') continue;
                for (int num = 1; num <=9;num++) {
                    if (!isValid(board,row,col,num)) {
                        continue;
                    }
                    board[row][col] = (char) ('0' + num);
                    if (backtracking(board)) return true;
                    board[row][col] = '.';
                }
                return false;
            }
        }
        return true;
    }

    public boolean isValid(char[][] board,int row ,int column,int num) {
        //判断数字在行上是否有效
        for (int i = 0; i < board.length; i++) {
            if (board[row][i] == (char)('0' + num)) {
                return false;
            }
        }
        //判断数字在列上是否有效
        for (int i = 0; i < board.length; i++) {
            if (board[i][column] == (char)('0' + num)) {
                return false;
            }
        }
        //判断数字在九宫格内是否有效
        int i = row / 3;
        int j = column / 3;
        for (int k = 0; k <3; k++) {
            for (int l = 0; l < 3; l++) {
                if (board[i * 3 + k][j * 3 + l] == (char)('0' + num)) {
                    return false;
                }
            }
        }
        return true;
    }
```





# 贪心算法

> 

## 核心思想

​	找到每个阶段的局部最优，从而推导出全局最优。

# 动态规划

简称DP,是一种通过把原问题分解为相对简单的子问题的方式求解复杂问题的方法．

<h3>核心思想</h3>

动态规划的核心思想是将原问题分解为多个相对简单的子问题，再根据子问题的解得出原问题的解。为了避免重复求解子问题，会将已经求解的子问题的结果记忆化存储，以便下次需要时直接查表。

通常具有以下性质的问题适合使用动态规划解决：

- 最优子结构性质，即问题的最优解可以由其子问题的最优解组合而成
- 无后效性，即子问题的解一旦确定，就不再改变，不受在这之后、包含它的更大的问题的求解决策影响。
- 子问题重叠性质，每次分解产生的子问题并不总是新问题，有些子问题会被重复计算多次。

## 应用

- 斐波那契数列
- 背包问题



## 动态规划五要点

- DP数组含义
- 递推公式
- DP数组初始化
- DP数组遍历顺序
- 打印DP数组

## 轮廓线DP

又称为插头DP，是一种

# 滑动窗口

**概念**：在数组或字符串上维护一个窗口（通常用两个指针 `left` 和 `right` 表示窗口的左右边界），窗口内的数据满足一定条件，窗口不断“滑动”以完成问题求解。

**目的**：减少重复计算，实现 **O(n)** 或接近 O(n) 的时间复杂度。

**核心操作**：

1. **扩大窗口**：移动右指针，包含更多元素，直到不满足条件。
2. **收缩窗口**：移动左指针，剔除部分元素，使窗口恢复满足条件。
3. **更新结果**：每次窗口调整后，根据题意更新答案（如长度、和、最大值等）。

# LRU算法

> `Least Recently Used`,最近最少使用。一种资源淘汰算法。

**核心思想**

> 哪个资源最近最久没被使用，就把他淘汰。

**应用场景**

- 缓存

**实现思路**

- 通过双向链表维护资源的最近使用情况，谁最近使用过，就将他插入链表头。
- 这样链表尾部就是天然的最近最少使用的数据

## LRU缓存

> Least Recently Used Cache（最近最少使用缓存），是一种根据`LRU`算法产生的缓存淘汰策略，用于管理有限容量的缓存，保证最常用的数据能够保留在缓存中，而不常用的数据被淘汰。

**核心思想**

​		如果缓存被占满，则淘汰**最近最少使用**的数据。

### 实现

- 通常使用双向链表与哈希表来实现LRU缓存
- 哈希表存储每一个缓存项的`key`,以及由`key`,`value`标识的双向链表节点
- 通过哈希表查询与存储缓存项，通过双向链表维护缓存项的最近使用情况，如果最近使用过，则其靠近头节点，否则则靠近尾节点。
- 哈希表的查询与写入时间复杂度均为O(1),双向链表移动节点位置以及删除节点的时间复杂度也为O(1)

```java
public class LRUCache {
    private HashMap<Integer,Node> map;
    private Node head;
    private Node tail;
    private int capacity;
    public LRUCache(int capacity) {
        map = new HashMap<Integer,Node>(capacity);
        this.capacity = capacity;
        head = new Node(0,0);
        tail = new Node(0,0);
        head.next = tail;
        tail.prev = head;
    }

    public int get(int key) {
        if (map.containsKey(key)) {
            Node node = map.get(key);
            node.prev.next = node.next;
            node.next.prev = node.prev;
            Node temp = head.next;
            head.next = node;
            node.next = temp;
            node.prev = head;
            temp.prev = node;
            return node.getValue();
        }
        return -1;
    }

    public void put(int key, int value) {
        if (map.containsKey(key)) {
            Node node = new Node(key,value);
            Node removeNode = map.remove(key);
            removeNode.prev.next = removeNode.next;
            removeNode.next.prev = removeNode.prev;
            map.put(key,node);
            Node temp = head.next;
            head.next = node;
            node.next = temp;
            node.prev = head;
            temp.prev = node;
            return;
        }
        if (map.size() == capacity) {
            int removeKey = tail.prev.getKey();
            map.remove(removeKey);
            tail.prev.prev.next = tail;
            tail.prev = tail.prev.prev;
        }
        Node node =new Node(key,value);
        map.put(key,node);
        Node temp = head.next;
        head.next = node;
        node.next = temp;
        node.prev = head;
        temp.prev = node;
    }
}
public class Node {
    private int key;
    private int value;
    public Node next;
    public Node prev;
    public Node(int key, int value) {
        this.key =key;
        this.value = value;
    }
    public int getKey() {
        return key;
    }
    public int getValue() {
        return value;
    }
}
```

# LFU算法

> `Least Frequently Used`,最近最不常用。一种资源淘汰算法，根据数据访问次数来淘汰数据，当访问次数一致时，删除最久未使用的数据。

**核心思想**

如果数据过去被访问过多次，那么将来被访问的频率也更高；反之亦然。

## LFU缓存

> `Least Frequency Used`,最近最不常用缓存，当缓存数据达到容量时，使用`LRU`算法淘汰数据。

# 协同过滤

协同过滤(Collaborative Filtering, CF)是推荐系统里最经典的一种实现方法，它的核心思想是利用历史行为数据找相似性。协同过滤可分为基于用户的协同过滤与基于物品的协同过滤

**基于用户的协同过滤**

基于历史行为寻找与用户相似的人，推荐与用户兴趣相似的人接触过而用户没有接触过的东西

**基于物品的协同过滤**

基于历史行为寻找相似的物品，推荐与用户感兴趣物品相似的物品

## ✅ 优点

- 不需要理解内容（简单）
- 推荐效果好（基于真实行为）
- 可扩展性强

## ❌ 缺点

1. **冷启动问题**
   - 新用户 → 没数据
   - 新物品 → 没人用
2. **数据稀疏**
   - 用户行为少 → 不好算相似度
3. **热门偏置**
   - 热门内容更容易被推荐（马太效应）

# 排序

## 快速排序

## 归并排序(`merge`)
归并排序的核心思想是最初将每个元素作为单独的有序子数组，每轮对相邻的子数组两两合并，直到只剩一个数组。

**特点**
- 稳定
- 平均，最好和最坏的时间复杂度均为O(nlog n)，空间复杂度为O(n)
- 既可以通过循环实现，也可以通过递归实现

# 压缩算法

压缩算法主要是利用数据的重复、规律或概率分布对其进行压缩。

## 分类

<h4>无损压缩</h4>

对数据解压后与压缩前的数据完全一致，没有任何损失。

**典型算法**

- 霍夫曼编码
- LZ77
- LZW

<h4>有损压缩</h4>

对数据解压后与压缩前的数据不完全一致，但在人类感知范围内。常用于图片，视频，音频等数据的压缩。

**典型算法**

- JPEG Compression
- MP3 Compression

## 压缩步骤

对于绝大部分无损压缩算法，其执行过程均可抽象为两步：

1. **建模：**发现或构造数据中的统计规律或结构冗余
2. **编码：**对建模后的数据进行编码，以用更少 bit 表示数据

## 霍夫曼编码

霍夫曼编码(`Huffman Coding`)是一种基于符号频率构建的最优前缀编码方法，广泛应用于数据压缩和通信编码，是无损压缩领域中重要的基础编码方法之一。

- 霍夫曼编码也可以视为一种压缩算法，但其压缩能力有限且依赖数据的概率分布。通常会使用其他算法对数据进行建模以增强数据的可压缩性，再使用霍夫曼编码对建模结果进行高效编码，从而获得更好更稳定的压缩效果

**特点**

- **前缀编码：**任意一个编码都不是另一个编码的前缀，可以无歧义解码
- **最优性：**在已知频率的情况下，霍夫曼编码在所有前缀编码方式中是平均编码长度最小的。
- **无损压缩：**可以完全恢复原数据

**核心思想**

用更短的编码表示出现频率高的符号，用较长的编码表示出现频率低的符号，从而减少整体数据长度。

<h3>基本原理</h3>

霍夫曼编码的核心是基于符号频率构建一个最优二叉树(又称为霍夫曼树)。

<h4>1.构建霍夫曼树</h4>

将每一个符号视为一个独立节点，重复以下操作：

- 每次选取频率最小的两个节点
- 将选取的两个节点合并成一个新节点（频率相加），原本的两个节点作为其叶子节点。
- 重复直到只剩一个节点

<h4>2.生成编码</h4>

- 从根节点开始，左分支记为 0，右分支记为 1（或反过来也可以）
- 从根到叶子的路径就是该字符的编码

## LZ77

基于滑动窗口的思想

### LZW

## `Floyd`判圈算法

# `Hash`算法

`Hash`(哈希)算法是一类映射算法的统称，它可以将任意长度的输入，映射为一个固定长度的输出。这个函数称为哈希函数，这里的输出通常称为哈希值(`Hash Value`)/摘要(`Digest`)/指纹(`Fingerprint`)。

`Hash`英文原意有剁碎，搅乱的含义，这里表示将原始数据经过某种搅乱压缩，得到一个固定长度短值。

<h2>常用<code>Hash</code>算法</h2>

<h3><code>MD5</code></h3>

将输入映射为128bit的固定输出。

- 计算速度快
- 已被破解，可构造碰撞

常用于：

- 非安全校验
- 文件去重

<h3><code>SHA-1</code></h3>

<h3><code>SHA-256</code></h3>

目前非常常用：

- HTTPS
- 区块链
- 数字签名

都大量使用。

<h3><code>MurmurHash</code></h3>

<h3><code>CRC32</code></h3>

<h3><code>SipHash</code></h3>

<h3><code>FNV</code></h3>

<h3><code>CityHash</code></h3>

<h3><code>xxHash</code></h3>

<h2>特点</h2>

理论上，任何能将任意输入映射为固定长度输出的函数，都可以算作哈希算法。但一个优秀的哈希算法，通常需要满足以下特性：

- 输入允许任意长度
- 无论输入多大，均输出固定长度
- **确定性：**相同输入永远可以得到相同输出
- **雪崩效应（Avalanche Effect）：**输入的微小变化会导致输出的巨大变化
- **不可逆：**无法通过哈希值反推出原数据
- **低冲突率：**发生哈希冲突的概率低

## 哈希冲突

`Hash`算法本质是将无限输入映射到有限输出，因此一定会存在两个不同的输入映射到同一个哈希值，这称为`Hash`冲突。

## 应用场景

<h3><code>Hash</code>表</h3>

`Hash`表是一种用于快速查找元素的数据结构。

它通常基于数组实现。在存储元素时，会根据元素的`hash`值通过取余运算映射到一个固定数组下标。在查找时，只需要再次执行这一计算即可获取元素位置，从实现O(1)时间复杂度的高效率查找。

<h3>数据去重</h3>

根据数据的哈希值判断数据是否重复，若哈希值相同，则大概率重复。

<h3>文件校验</h3>

提前计算出文件的哈希值，只要文件被修改，哈希值就会发生巨大变化，从而验证文件是否被篡改。

<h3>密码存储</h3>

数据库中通常不会直接存储明文密码，而是会存储密码的哈希值。由于哈希函数是不可逆的，即使数据库泄露也不会直接暴露密码

<h3>区块链</h3>

提前计算出区块数据的哈希值，如果区块数据被修改，则哈希值发生编码，从而检测篡改。

<h3>数字签名</h3>

# 共识算法

共识算法是一类用于保证分布式系统数据强一致性的算法，包括 `Paxos`、`Raft` 等。

## `Raft`算法

`Raft`算法是一种共识算法，用于解决分布式系统中多个节点间的数据一致性问题。

- `Raft`的含义是`Reliable, Replicated, Redundant, And Fault-Tolerant`（可靠、可复制、可冗余、可容错）

**特点**

- 易理解，易实现
- 强一致性。
- 自动容错
- `Leader`单点写入

**核心思想**

`Raft` 的核心思想是在分布式系统中选举出一个 Leader 节点，所有写入操作由 Leader 统一处理，并通过日志复制机制同步到其他 Follower 节点；当 Leader 故障时，通过重新选举保证系统继续工作，从而简化分布式一致性问题。

------

在`Raft`算法中，分布式系统中的节点具有三种状态：

**`Leader`(领导者)**

负责处理所有客户端请求，并把日志复制给其他节点以同步状态。

**`Follower`(跟随者)**

不会处理客户端请求，接收 Leader 的日志并同步状态。

**`Candidate`(候选人)**

当系统中的`Leader`失去心跳后，`Follower`会转换为`Candidate`，进行选举，争取成为`Leader`。

整个分布式系统中只能有一个领导者，正常情况下集群架构如下：

```
        Client
           |
        Leader
       /   |   \
Follower Follower Follower
```

### 工作机制

<h4>领导选举</h4>

1. `Follower` 在超时时间内没收到 `Leader` 心跳
2. 转为 `Candidate`
3. 向其他节点发起投票（`RequestVote`）
4. 如果得票超过一般，则成为 `Leader`

<h4>日志复制</h4>

1. Leader 接收客户端请求后，将操作封装为日志条目写入本地日志
2. Leader 将日志复制到 Follower，Follower 持久化但不执行
3. 当日志被多数节点确认后，该日志被标记为 committed
4. 所有节点按照 commit 顺序依次执行日志

**日志结构**

每条日志包含三部分信息：

- index（编号）
- term（任期）
- command（操作）

<h4>安全性保证</h4>

<h4>心跳机制</h4>

`Leader`定期向其他`Follower`发送 `AppendEntries`（空日志），以防止`Follower`超时选举，确保领导地位。


# 其他算法

<h3>获得大于等于指定整数cap的最小2的幂</h3>

**位扩散法**

```java
    public static int tableSizeFor(int cap) {
        //处理 cap 本身就是 2 的幂的情况
        int n = cap - 1;
        //把最高位的 1 后面的所有位都变成 1
        n |= n >>> 1;
        n |= n >>> 2;
        n |= n >>> 4;
        n |= n >>> 8;
        n |= n >>> 16;
        return n > 0 ? n + 1 : 1;
    }
```

- 将 `cap-1` 的最高位 1 向右扩散成全 1，再加 1，从而在 O(1) 时间内计算出 **大于等于 cap 的最小 2 的幂容量**

<h3>整数除法向上取整的小技巧</h3>

各种编程语言中的整数除法默认为向下取整，这里给出一个无需转换小数的向上取整技巧

```java
int dividend; //被除数
int divisor;  //除数
res = (dividend + divisor - 1) / divisor;
```

